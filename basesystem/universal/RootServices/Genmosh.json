{
    "softwareName": "Node",
    "staticPort": 1000,

    "start": {
        "commands": ["test -d /SystemUsers/WebApps/Genmosh && cd /SystemUsers/WebApps/Genmosh && sofin deps && npm install && coffee ./app.coffee"]
    },
    "stop": {
        "commands": ["test -e SERVICE_PREFIX/service.pid && kill -TERM $(cat SERVICE_PREFIX/service.pid) && rm -f SERVICE_PREFIX/service.pid"]
    }
}
