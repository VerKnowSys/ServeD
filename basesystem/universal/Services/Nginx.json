{
    "name": "Nginx",
    "reportAllInfos": true,
    "install": {
        "commands": ["sofin get nginx"],
        "expectStdOut": ["Preparing application: Nginx", "All done."],
        "expectOutputTimeout": 360
    },
    "configure": {
        "commands": ["printf '
worker_processes  2;
events {
    worker_connections  1024;
}

http {
    include       SERVICE_ROOT/conf/mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    keepalive_timeout  270;
    server {
        listen       SERVICE_PORT;
        server_name  localhost;
        location / {
            root   SERVICE_ROOT/html;
            index  index.html index.htm;
        }
    }
}

' > SERVICE_PREFIX/service.conf"]
    },
    "start": {
        "commands": ["nginx -c SERVICE_PREFIX/service.conf"]
    },
    "stop": {
        "commands": ["nginx -c SERVICE_PREFIX/service.conf -s stop"]
    }
}