#!/bin/zsh

if which pygmentize > /dev/null; 
then
else
    easy_install pygments
fi

if gem list jekyll | grep jekyll > /dev/null;
then
else
    gem install jekyll
fi

echo "Generating site"
jekyll --pygments docs/site target/_site
