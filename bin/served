#!/bin/bash
# This Software is a close code project. You may not redistribute this code without permission of author.

. bin/OPTS

args=""

bin/installAll

if [ "$?" == "0" ]; then

  cd src/svd.maintainer/
  $MVN_EXEC $MVN_PARAMS exec:java -Dexec.args=$args $@
  
fi

exit 0
